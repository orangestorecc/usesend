import { PageStylePanel } from "./PageStylePanel";

/**
 * Painel direito. Por ora sempre mostra o estilo da página; a fase seguinte
 * troca para as propriedades do bloco quando houver um selecionado.
 */
export function PropertiesPanel() {
  return <PageStylePanel />;
}
